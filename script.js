/* Global styles for the entire page */ body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #f0f2f5; color: #333333; margin: 0; padding: 0; display: flex; flex-direction: column; min-height: 100vh; align-items: center; justify-content: center; } /* Header styling */ header { text-align: center; margin-bottom: 20px; } header h1 { color: #2563eb; margin-bottom: 5px; } /* Card container styling */ .card { background-color: #ffffff; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1); text-align: center; max-width: 400px; } /* Button styling */ button { background-color: #2563eb; color: white; border: none; padding: 10px 20px; font-size: 16px; border-radius: 6px; cursor: pointer; transition: background-color 0.2s ease; margin-top: 15px; } /* Button hover effect */ button:hover { background-color: #1d4ed8; } /* Footer styling */ footer { margin-top: 30px; font-size: 14px; color: #6b7280; }
// Select DOM elements from our HTML document const miniGameBtn = document.getElementById('minigame-btn'); const gameModal = document.getElementById('game-modal'); const closeModal = document.getElementById('close-modal'); const guessBtn = document.getElementById('guess-btn'); const userGuessInput = document.getElementById('user-guess'); const gameMessage = document.getElementById('game-message'); // Generate a random secret number between 1 and 10 for the mini-game let targetNumber = Math.floor(Math.random() * 10) + 1; // Open the mini-game modal when the user clicks the left-side button miniGameBtn.addEventListener('click', function() { gameModal.classList.remove('hidden'); }); // Close the mini-game modal when the user clicks the 'X' button closeModal.addEventListener('click', function() { gameModal.classList.add('hidden'); resetGame(); }); // Close the modal if the user clicks anywhere outside of the modal box window.addEventListener('click', function(event) { if (event.target === gameModal) { gameModal.classList.add('hidden'); resetGame(); } }); // Check the user's guess when they click the 'Submit Guess' button guessBtn.addEventListener('click', function() { const userGuess = Number(userGuessInput.value); // Validate that the input is between 1 and 10 if (!userGuess || userGuess < 1 || userGuess > 10) { gameMessage.textContent = "Please enter a valid number between 1 and 10!"; gameMessage.style.color = "orange"; return; } // Compare user's guess with the secret target number if (userGuess === targetNumber) { gameMessage.textContent = "🎉 Correct! You guessed the right number!"; gameMessage.style.color = "green"; // Generate a brand new number so they can play again immediately targetNumber = Math.floor(Math.random() * 10) + 1; } else if (userGuess < targetNumber) { gameMessage.textContent = "📈 Too low! Try a higher number."; gameMessage.style.color = "red"; } else { gameMessage.textContent = "📉 Too high! Try a lower number."; gameMessage.style.color = "red"; } }); // Helper function to clear input fields and reset the game state function resetGame() { userGuessInput.value = ""; gameMessage.textContent = ""; targetNumber = Math.floor(Math.random() * 10) + 1; }
